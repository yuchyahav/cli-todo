#pragma once

namespace todo {
enum class MenuOptions {
  EXIT = 0,
  ADD = 1,
  REMOVE = 2,
  CHANGE_STATUS = 3,
  CHANGE_PRIO = 4,
  UNDO = 5,
  REDO = 6,
  CLEAR = 7,
  INVALID = 8
};
}  // namespace todo
